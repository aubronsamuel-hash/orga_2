[project]
name = "ccw"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic>=2",
    "pydantic-settings",
    "alembic",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
